# ============================================
# Firebase Configuration (Required for Authentication)
# ============================================
# Get these values from your Firebase Console:
# 1. Go to https://console.firebase.google.com/
# 2. Create a new project or select existing project
# 3. Go to Project Settings > General
# 4. Under "Your apps" section, create a Web app if not already created
# 5. Copy the Firebase configuration values below

NEXT_PUBLIC_FIREBASE_API_KEY=your_firebase_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_firebase_app_id

# ============================================
# Legacy Authentication (Optional - for backward compatibility)
# ============================================
# These are no longer used with Firebase but can be kept for fallback
# AUTH_USERNAME=admin
# AUTH_PASSWORD=your_secure_password

# ============================================
# AI Provider API Keys (Optional - for server-side fallback)
# ============================================
# Users will be prompted to enter their API keys in the UI.
# These environment variables serve as fallback if users don't provide keys.
# You can leave these empty if you want users to always provide their own keys.

# Google Gemini API Key (Optional)
# Get it from: https://aistudio.google.com/apikey
GEMINI_API_KEY=

# OpenAI API Key (Optional)
# Get it from: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# ============================================
# Gemini Rate Limiting (Optional)
# ============================================
# Adjust these based on your Gemini API tier
# Free tier: 15 RPM, 1500 RPD
# Paid tier: 60+ RPM, 10000+ RPD

# GEMINI_RPM_LIMIT=15
# GEMINI_RPD_LIMIT=1500

# ============================================
# OpenAI Rate Limiting & Cost Control (Optional)
# ============================================
# Adjust these based on your OpenAI account tier

# OPENAI_RPM_LIMIT=100         # Requests/min (tier-based)
# OPENAI_RPD_LIMIT=10000       # Requests/day
# OPENAI_MAX_COST_HOUR=0.30    # Max cost/hour in USD (default: $0.30 for single user)
