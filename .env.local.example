# ============================================
# Firebase Configuration (REQUIRED for Authentication)
# ============================================
# Get these values from your Firebase Console:
# 1. Go to https://console.firebase.google.com/
# 2. Create a new project or select existing project
# 3. Go to Project Settings > General
# 4. Under "Your apps" section, create a Web app if not already created
# 5. Copy the Firebase configuration values below
#
# ⚠️ REQUIRED: The app will NOT work without these values

NEXT_PUBLIC_FIREBASE_API_KEY=your_firebase_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_firebase_app_id

# ============================================
# Legacy Authentication (NOT NEEDED - Deprecated)
# ============================================
# These are NO LONGER USED with Firebase authentication.
# The app now uses Firebase for all user authentication.
# You can safely leave these commented out or remove them.

# AUTH_USERNAME=admin
# AUTH_PASSWORD=your_secure_password

# ============================================
# AI Provider API Keys (OPTIONAL - For Server-Side Fallback Only)
# ============================================
# Users will be prompted to enter their API keys in the UI after login.
# These environment variables serve ONLY as fallback if users don't provide keys.
#
# ℹ️ RECOMMENDED: Leave these EMPTY and let users provide their own keys via UI
# This ensures users control their own API usage and costs.
#
# Only set these if you want to provide server-side API keys as fallback.

# Google Gemini API Key (Optional - Leave empty for user-provided keys)
# Get it from: https://aistudio.google.com/apikey
GEMINI_API_KEY=

# OpenAI API Key (Optional - Leave empty for user-provided keys)
# Get it from: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# ============================================
# Gemini Rate Limiting (OPTIONAL - Advanced Configuration)
# ============================================
# These settings control server-side rate limiting for Gemini API.
# Users can also configure their own limits via the Settings page in the UI.
#
# ℹ️ NOT NEEDED for most users - Use UI settings instead
# Only uncomment if you need server-wide rate limiting.
#
# Default values (if not set):
# - Free tier: 15 RPM, 1500 RPD
# - Paid tier: 60+ RPM, 10000+ RPD

# GEMINI_RPM_LIMIT=15
# GEMINI_RPD_LIMIT=1500

# ============================================
# OpenAI Rate Limiting & Cost Control (OPTIONAL - Advanced Configuration)
# ============================================
# These settings control server-side rate limiting and cost tracking for OpenAI API.
# Users can also configure their own limits via the Settings page in the UI.
#
# ℹ️ NOT NEEDED for most users - Use UI settings instead
# Only uncomment if you need server-wide rate limiting or cost control.
#
# Default values (if not set):
# - Rate limits: 100 RPM, 10000 RPD (adjust based on your OpenAI tier)
# - Max cost: $0.30/hour (optimized for single user)

# OPENAI_RPM_LIMIT=100         # Requests/min (tier-based)
# OPENAI_RPD_LIMIT=10000       # Requests/day
# OPENAI_MAX_COST_HOUR=0.30    # Max cost/hour in USD
