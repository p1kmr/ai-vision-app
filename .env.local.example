# Authentication Credentials (Required)
AUTH_USERNAME=admin
AUTH_PASSWORD=your_secure_password

# AI Provider API Keys
# Add at least one API key to use the application

# Google Gemini API Key (optional - for Gemini provider)
# Get it from: https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# OpenAI API Key (optional - for OpenAI provider)
# Get it from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Gemini Rate Limiting Configuration (optional)
# Free tier: 15 requests/min, 1500 requests/day (already optimized)
# Uncomment and adjust these ONLY if you have a paid tier
# GEMINI_RPM_LIMIT=15
# GEMINI_RPD_LIMIT=1500
#
# Free Tier Optimization Features (Already Applied):
# ✅ Automatic rate limiting (15 RPM, 1500 RPD)
# ✅ Request queuing with exponential backoff
# ✅ Reduced max tokens: 2048 (from 8192 to save quota)
# ✅ Lower temperature (0.7) and top_k (40) for focused responses
# ✅ Minimal system prompts to save tokens
# ✅ Daily quota warnings at 80% and 90%
# ✅ Real-time quota tracking every 30 seconds
#
# For paid tier, uncomment and set:
# GEMINI_RPM_LIMIT=60
# GEMINI_RPD_LIMIT=10000

# OpenAI Rate Limiting & Cost Control (optional)
# Tier-based rate limits (adjust based on your OpenAI tier)
# OPENAI_RPM_LIMIT=100          # Requests per minute (default: 100)
# OPENAI_RPD_LIMIT=10000        # Requests per day (default: 10000)
# OPENAI_MAX_COST_HOUR=0.30     # Max cost per hour in USD (default: $0.30 for single user)

# Cost Optimization Features (Already Applied):
# ✅ gpt-4o-mini-realtime as default (~4x cheaper than gpt-4o)
# ✅ Ultra-minimal system prompt ("AI") to reduce per-request costs
# ✅ Reduced max tokens: 1024 (50% reduction from previous default)
# ✅ Lower temperature (0.7) for shorter, focused responses
# ✅ Server-side VAD prevents billing during silence
# ✅ Cost warnings at 50% and 80% of hourly limit
# ✅ Automatic hourly cost tracking and limits
#
# Pricing (approximate):
# - gpt-4o-mini-realtime: ~$0.06/min (~$0.30 = 5 minutes)
# - gpt-4o-realtime: ~$0.30/min (~$0.30 = 1 minute)
#
# For single-user testing: Keep default $0.30/hour
# For regular use: Increase to $1.00-$2.00/hour
# OPENAI_MAX_COST_HOUR=0.30

# Notes:
# - You can use either provider or both
# - If you only want to use Gemini, just add GEMINI_API_KEY
# - If you only want to use OpenAI, just add OPENAI_API_KEY
# - Rate limiting is automatically enabled for Gemini free tier
# - For Gemini paid tier, set GEMINI_RPM_LIMIT=60 and GEMINI_RPD_LIMIT=10000
# - OpenAI cost tracking resets hourly and logs usage to console
# - Copy this file to .env.local and fill in your actual keys
